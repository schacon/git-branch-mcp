# Some Ideas

- with updateBranch, attempt to determine if changes should be absorbed first
  - the prompt is similar to a commit message already committed
- use AI to amend commits
  - if there is only one commit, simply amend it
